{
  "name": "reading-battler",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Word Quest — a turn-based reading battler for early readers",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-transform-types tests/*.test.ts"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
